server:
  port: 8081

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SantiMolina9/repo-config-server
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

